-- 1. ★ Napisz skrypt składający się z poleceń psql, który wykona zapytanie
-- (użyj dowolnego zapytania z zadania 3.6, ale innego niż w zadaniu 3.7) oraz zwróci jego wynik jako dokument tekstowy z polami oddzielonymi przecinkami
-- (Comma Separated Values), z jednym rekordem w jednej linii.

-- UWAGA: Przydatne polecenia: \a \pset fieldsep \t

psql ... < zapytanie.sql > wynik.txt